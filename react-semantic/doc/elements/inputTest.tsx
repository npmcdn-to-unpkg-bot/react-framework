import * as React from 'react';


import {
  Icon, flipped, rotated, circularIcon, bordered,
  Input, InputProps, iconInput, labeled, action,
} from '../../common/exports';

import * as ui from '../../common/exports';

export const InputTest: ui.StatelessComponent<ui.IProps> = props => {
  return <div>
    
  </div >;
}